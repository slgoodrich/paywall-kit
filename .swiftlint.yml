# SwiftLint owns lint smells; SwiftFormat owns pure formatting.
# Formatting-overlap rules are disabled to avoid fighting SwiftFormat.
excluded:
  - .build

disabled_rules:
  - trailing_comma # SwiftFormat manages trailing commas

line_length:
  warning: 120
  error: 200
